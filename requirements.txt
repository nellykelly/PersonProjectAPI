# Ensures zoneinfo("America/New_York") resolves on any platform/container
# (Windows and slim Linux images don't reliably ship IANA tz data).
tzdata>=2024.1

Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1,<4.0
Flask-Limiter>=3.8,<4.0

# Accounts: a username/password login gating the LeetCode 150 tracker so
# each account keeps its own board (app/blueprints/auth, models.User /
# models.LeetCodeProgress). Flask-WTF is here for its CSRF protection on
# the auth + tracker-progress POSTs -- the older public-write blueprints
# stay csrf-exempt for now (see create_app).
Flask-Login>=0.6,<0.7
Flask-WTF>=1.2,<2.0
yfinance>=0.2.40,<0.3.0
requests>=2.32,<3.0
python-dotenv>=1.0,<2.0
gunicorn>=22.0,<23.0

# Pipeline World / SRE Infra Layer
redis>=5.0,<9.0
rq>=1.16,<3.0
Flask-SocketIO>=5.3,<6.0
psycopg2-binary>=2.9,<3.0
Flask-Migrate>=4.0,<5.0

# Real runtime dependency, not dev-only: app/services/queue.py falls back
# to this whenever REDIS_URL is unset (the documented no-Docker dev path,
# and every SSE endpoint's concurrency cap in sse_limits.py also needs a
# Redis connection) -- a production install without it crashes on boot.
fakeredis>=2.20,<3.0

# Personal AI assistant (app/services/assistant, app/blueprints/assistant).
# All three are imported lazily -- the app still boots, and the test suite
# still runs, without them installed (the /assistant page just reports
# "offline"). Retrieval requires Postgres + the pgvector extension, same
# as /pipeline-analytics; there is no SQLite path for it.
groq>=0.20,<1.0          # Groq LPU chat inference + function calling, free tier
fastembed>=0.3,<0.8      # local ONNX sentence embeddings (bge-small-en-v1.5, 384-d), no torch
pgvector>=0.3,<0.5       # SQLAlchemy vector column type + `<=>` cosine operator on Postgres

# Market Data Warehouse (app/blueprints/market_warehouse, app/services/
# market_warehouse.py). Imported lazily -- the /projects/market-warehouse
# page just reports "not available" without it installed, same pattern as
# the assistant deps above. Reads the sibling market-data-warehouse/
# project's DuckDB file read-only; also works unmodified against a
# MotherDuck ("md:...") path.
duckdb>=1.0,<2.0
