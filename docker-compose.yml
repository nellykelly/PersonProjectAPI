services:
  web:
    build: .
    image: personal-site:latest
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      FLASK_ENV: production
    volumes:
      - ./instance:/app/instance
    restart: unless-stopped
