"""add risk model_key and result report

Revision ID: a452e7b14c7a
Revises: 6f37e11d0712
Create Date: 2026-08-23 01:10:18.766170

"""
from alembic import op
import sqlalchemy as sa


# revision identifiers, used by Alembic.
revision = 'a452e7b14c7a'
down_revision = '6f37e11d0712'
branch_labels = None
depends_on = None


def upgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table('risk_requests', schema=None) as batch_op:
        # server_default is required, not optional: rows already exist, and
        # Postgres backfills them with NULL before validating the NOT NULL
        # constraint, so adding this bare would fail outright. Every
        # existing run really was Trader Granular (it was the only model
        # there was), so that is the honest backfill value. The default is
        # dropped straight after, since the ORM always sets it explicitly.
        batch_op.add_column(
            sa.Column('model_key', sa.String(length=40), nullable=False,
                      server_default='trader_granular')
        )
        batch_op.alter_column('model_key', server_default=None)

    with op.batch_alter_table('risk_results', schema=None) as batch_op:
        batch_op.add_column(sa.Column('report', sa.JSON(none_as_null=True), nullable=True))

    # ### end Alembic commands ###


def downgrade():
    # ### commands auto generated by Alembic - please adjust! ###
    with op.batch_alter_table('risk_results', schema=None) as batch_op:
        batch_op.drop_column('report')

    with op.batch_alter_table('risk_requests', schema=None) as batch_op:
        batch_op.drop_column('model_key')

    # ### end Alembic commands ###
